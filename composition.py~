def Composition(k, Text):
    arq = open("resp.txt", 'w')
    comp = []
    for i in range(len(Text)-k+1):
        comp.append(Text[i:i+k])
    comps = comp.sort()
    arq.write('\n'.join(comp))

a = open("data.txt")
print a
k = int(a.readline())
t = a.readline()
Composition(k,t)
